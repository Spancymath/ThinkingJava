package chapter03;

/**
 * 操作符
 * 1.基本类型存储了实际的数值，为其赋值，是直接将一个地方的内容复制到了另一个地方。例如对基本类型使用a=b，那么b的内容就复制给a，若接着又修改了a，b根本不会受这种修改的影响。
 * 2.当为一个对象赋值的时候，我们真正操作的是对象的引用。若对对象使用c=d，那么c和d都指向原本只有d指向的那个对象。
 * 3.要想执行类型转换，需要将希望得到的数据类型置于圆括号内，放在要进行类型转换的值的左边。
 *
 */
public class AnswerList {
}
